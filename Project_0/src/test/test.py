'''
map(lambda x_w: x_w[0] * x_w[1],zip(input_vec, self.weights))

'''


def __pulus_one(n):
    return n + 1


def map_test():
    v_m = [0.0 for _ in range(3)]

    print(list(v_m))


def f_act(i):
    return 1 if i > 0 else 0


print(f_act(3))
